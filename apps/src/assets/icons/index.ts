/**
 * Icon Components - Central Export
 *
 * This module exports all icon components used across the application.
 * Icons are organized by category and easily accessible via simple imports.
 *
 * @example
 * ```tsx
 * import { MealIcon } from '@/assets/icons';
 *
 * const MyComponent = () => (
 *   <MealIcon name="pizza" size={32} color="#212121" />
 * );
 * ```
 */

export { MealIcon, type MealIconProps } from './meal-icons';
